interface:
  display_name: "Loop Paper"
  short_description: "Propose, validate, combine, and close hypothesis-first paper work loops."
  default_prompt: "Initialize a paper stack, research and propose candidates for human selection, run gated paper loops with attested evidence, review and combine papers."
